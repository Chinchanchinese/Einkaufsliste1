<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.rg.einkaufsliste" >

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Einkaufskorb_Activity"
            android:label="@string/title_activity_einkaufskorb_"
            android:theme="@style/AppTheme.NoActionBar" >
        </activity>
        <activity
            android:name=".GerichteEingabeActivitySave"
            android:label="@string/title_activity_gerichte_eingabe"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".GerichteEingabeActivity"
            android:label="@string/title_activity_gerichte_eingabe"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ZutatenEingabeActivity"
            android:label="@string/title_activity_zutaten_eingabe"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ZutatenActivity"
            android:label="@string/title_activity_zutaten"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="de.rg.einkaufsliste.provider"
            android:exported="false"
            android:grantUriPermissions="true" >
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>
    </application>

</manifest>